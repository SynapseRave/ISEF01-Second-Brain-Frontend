import 'package:flutter/material.dart';

/// Suchseite: Dienstübergreifende Suche über alle verbundenen Dienste.
/// TODO(phase-6): SearchCubit + Debounce + Service-Aggregation implementieren.
class SearchPage extends StatelessWidget {
  const SearchPage({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Suche — Phase 6')));
}
