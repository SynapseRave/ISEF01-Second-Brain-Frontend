import 'package:flutter/material.dart';

/// Chat-Screen mit Streaming-Antworten.
/// TODO(phase-7): Eingabefeld, Nachrichten-Liste und Streaming implementieren.
class ChatPage extends StatelessWidget {
  const ChatPage({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Chat — Phase 7')));
}
